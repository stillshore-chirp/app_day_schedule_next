{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "analog-clock",
  "description": "Analog clock window invokes only allowlisted application commands.",
  "windows": ["analog-clock"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
