# Day Schedule Next ユーザーマニュアル

この文書は一般ユーザー向けの現行操作仕様です。Day Schedule Next は、予定をまず端末内の SQLite へ保存するローカルファーストのデスクトップアプリです。

## 1. 最初に確認する場所

起動すると「今日」画面を開きます。

- 画面上部: 表示日、前後の日、今日へ戻る、検索、予定追加、同期状態
- 24時間ストリップ: 共通の00〜24時軸で「今日の予定」と選択中の「日次テンプレート」を比較
- 詳細タイムライン: 分単位の作成・移動・リサイズ
- 画面下部の Now: 現在の予定、残り、次の予定、次のアラーム、Focus
- 左ナビゲーション: 初期状態はアイコンだけの細い表示です。上端の矢印で名称付きの表示へ展開でき、展開した選択は次回起動時にも維持されます。各アイコンにカーソルを合わせると画面名を確認できます。項目は今日、週、月、一覧、テンプレート、チケット、フォーカス、タイマー、ストップウォッチ、アラーム、設定、データと診断です。

予定がない日も24時間ストリップと日次テンプレートは表示され、上段の「予定を作成」から最初の予定を追加できます。テンプレートの読込中・失敗は下段だけに表示され、上段の予定確認と詳細タイムラインはそのまま利用できます。検索は上段の予定だけを対象とし、参照中の日次テンプレートを絞り込みません。検索結果なし、オフライン、再試行待ち、競合、権限拒否、失敗は別の状態として表示します。

## 2. 予定を作成・編集する

「＋予定」、詳細タイムラインの空き領域、または `Command/Ctrl + N` で作成パネルを開きます。上からタイトル、日付・時刻、説明を入力し、それだけで「予定を作成 / 変更を保存」で完了できます。保存とキャンセルはパネル下部に表示され続けます。

時刻は1分単位で直接入力するか、「開始時刻の候補 / 終了時刻の候補」プルダウンから選びます。開始時刻を変えると所要時間を保ったまま終了も移動し、終了時刻を変えると所要時間が変わります。「時刻を移動」で開始・終了を同じ分だけ前後させ、「所要時間」で短縮・延長、または15分・30分・60分へ切り替えられます。同日の予定は日付を1回だけ入力し、日をまたぐ場合は「翌日」と終了日を表示します。

優先度、終日、繰り返し、タイムゾーン、通知、分類、場所などは「詳細設定」を開いて入力します。設定済みの項目がある場合は「設定あり」を表示します。

### 説明を通常表示・Markdown表示・編集する

予定とチケットの説明は、Markdownを解釈しない通常表示と、書式を反映するMarkdown表示を選べます。意図せずMarkdown記号として解釈される本文は通常表示で原文の形を確認し、長い手順、比較表、確認項目を構造化して読む場合はMarkdown表示を使います。

- 説明がある項目を開くと「通常プレビュー」、空の新規項目は「編集」から始まります。「通常プレビュー / Markdownプレビュー / 編集」はクリックに加え、切替へフォーカスした状態で左右矢印、Home、Endでも選べます。
- 「通常プレビュー」はMarkdownやHTMLを解釈せず、改行と連続空白を保って本文を表示します。本文中の `http` / `https` URLはクリックまたはキーボード操作で既定ブラウザに開きます。URLの直後に文章を続ける場合は、空白、句読点、または括弧で区切ってください。
- 「Markdownプレビュー」は見出し、太字・斜体、箇条書き、番号付きリスト、チェックリスト、表、引用、インラインコード、コードブロック、リンク表示に対応します。幅の広い表とコードはプレビュー内を横スクロールします。
- 2種類のプレビューは保存操作ではありません。原文を変更する場合は「編集」へ戻り、予定では「予定を作成 / 変更を保存」、チケットでは「保存」を選びます。切り替えただけでは入力は失われません。
- raw HTMLは通常プレビューでは文字として表示し、Markdownプレビューでは実行・表示しません。Markdownリンクはリンク先を文字として併記します。`http` / `https`以外の形式はリンクとして扱いません。外部画像は通信と追跡を防ぐため読み込まず、Markdownプレビューでは代替テキストだけを表示します。
- Google Calendarへ同期する予定は、Markdown原文を通常の説明文として送ります。Google側で書式表示されなくても、Day Schedule Nextへ戻るとプレビューできます。チケット本文はGoogleへ同期せず、端末内に保持します。
- 表示が意図と異なる場合は「通常プレビュー」または「編集」で原文を確認してください。Markdownを使わない通常の文章も、そのまま説明として保存・表示できます。

- 保存精度は1分です。タイムラインのスナップ幅は設定で変更できます。
- 開始と終了は半開区間 `[start, end)` として扱います。
- 終日予定は開始日と終了日（終了日を含まない）を保持します。
- DST で存在しない時刻や2通りになる時刻は黙って補正せず、選択または時刻変更を求めます。
- 再発予定の変更・削除では「この予定」「これ以降」「シリーズ全体」の対象を確認します。

選択した予定は複製・削除できます。削除やテンプレート適用は履歴へまとまり、画面の「元に戻す」「やり直す」で回復できます。Google 同期対象を元に戻した場合は、古い未送信操作を無効化し、復元後の新しい版を Outbox へ積み直します。

### タイムライン操作

- 空き領域をドラッグ: 範囲を指定して新規作成
- 予定本体をドラッグ: 開始・終了を同じ長さで移動
- 上端／下端をドラッグ: 開始／終了をリサイズ
- `Esc`: ドラッグ中の変更を取消
- 選択した予定で `↑` / `↓`: スナップ幅だけ移動
- `Shift + ↑` / `Shift + ↓`: 終了だけ調整
- `Option/Alt + ↑` / `Option/Alt + ↓`: 開始だけ調整
- `Alt + ←` / `Alt + →`: 表示日を移動

すべてのドラッグ操作には時刻入力とキーボード操作の等価手段があります。

24時間ストリップの予定バーは、各レーン内の開始時刻順に番号、開始時刻、タイトルを表示します。バーの幅に応じて3行表示から詰めた表示へ切り替わりますが、短い予定でもタイトルを番号だけに置き換えません。収まりきらないタイトルは末尾に省略記号を付けず、バーの幅まで表示します。完全なタイトルと開始・終了時刻は、バーの読み上げ名、`title`、編集パネルで確認できます。詳細タイムラインでは表示高さが小さいと1行表示へ切り替わります。

## 3. 週・月・一覧を使う

- 週: 7日分の配置と日ごとの件数を確認
- 月: 日ごとの予定を俯瞰し、日付を選択
- 一覧: 期間、状態、プロジェクト、カテゴリ、タグ、優先度、同期状態、同期先、完了状態で絞り込み、並べ替え・ページ移動

一覧検索はタイトル、説明、場所、プロジェクト、カテゴリ、タグを対象にします。1ページ100件で表示し、現在のページまたは複数ページから選んだ予定のプロジェクト、カテゴリ、タグ、色、優先度を最大500件まで一括変更できます。有効にした項目だけが変わり、一括変更全体を1回の Undo で回復できます。読み取り専用の Google 予定が含まれる場合は部分適用せず、選択の見直しを求めます。

## 4. テンプレートと Quick Block

テンプレートでは、複数の「一日の型」を追加・編集・複製・並べ替えできます。削除できない既定テンプレートが常に1件あり、その名前は変更できません。最後に選んだテンプレートは再起動後も復元されます。

各ブロックは24時間ストリップと詳細タイムラインで確認し、1分単位のスライダー、時刻・所要分入力、色、プロジェクト、カテゴリを編集できます。1440分までの日跨ぎに対応します。

「今日」の24時間ストリップでは、保存済み予定と有効な Quick Block を上段、最後に選んだ日次テンプレートを下段に表示します。各バーはレーン内の開始時刻順に番号、開始時刻、タイトルを内部へ表示し、幅に合わせて情報を詰めます。予定を主役として読み取りやすくするため、種別と名前は左側の見出し領域へ分け、テンプレート編集はストリップ見出しの下の「テンプレートを編集」へ置いてバーの横幅を保ちます。上下は同じ00〜24時軸を使いますが、選択・編集の対象は分離されています。下段のブロックは参照専用で、クリック、ドラッグ、リサイズ、予定への暗黙適用を行いません。「テンプレートを編集」からテンプレート画面へ移動すると、選択中のテンプレートをそのまま編集できます。

保存済みの選択対象が削除されていた場合は並び順の先頭を表示します。テンプレートがない場合や時間ブロックがない場合は、下段に理由と作成・編集導線を表示します。24:00を越えるブロックは当日の右端で切り詰め、「翌日へ継続」と表示します。Todayに表示しただけでは予定作成、テンプレート適用、Google同期は行いません。

日付へ適用する前に、次をプレビューします。

- 生成予定の件数と時刻
- 既存予定と重なる生成予定
- 置換対象になるローカル予定
- 保持する Google 由来の予定
- 同期先

「追加」は既存予定を残します。「置換」は対象日のローカル予定だけを置換し、Google 由来の予定を削除しません。適用全体を1操作として Undo できます。

Quick Block はテンプレートと独立した毎日の定常ブロックです。追加・編集・複製・削除・並べ替え、時刻、所要分、色、分類、開始／終了通知を管理できます。無効にすると24時間表示、Now、通知から外れますが、データは残ります。

## 4.1 チケットをKanbanで整理する

「チケット」は「何を完了するか」、予定は「いつ実行するか」を分けて扱います。Inbox、Backlog、Next、In Progress、Waiting、Done の6列で次に進める一件を判断し、必要になった時点で1件のチケットへ複数の予定を割り当てられます。

- 各列の下部ではタイトルだけで素早く作成できます。画面上部の「チケットを作成」はInboxへ作ります。
- カードの詳細では説明、優先度、期限、見積時間、タグ、チェックリストを編集します。説明の表示切替と安全上の制約は「説明を通常表示・Markdown表示・編集する」と共通です。保存中、保存済み、失敗、ほかの変更との競合は別々の結果バナーで表示され、失敗時も入力を保持します。詳細は閉じるボタン、`Esc`、またはモーダルの外側をクリックして閉じられます。未保存の入力がある場合は、破棄するか確認します。
- カード詳細の「予定へ割り当て」で開始日時と所要時間を指定すると、新しい予定を作成して関連付けます。関連予定の件数、今後の計画時間、次の開始を確認でき、不要になった関連だけを解除できます。関連解除は予定やチケットを削除しません。
- 「今日」の未配置チケットを開くと、今日以降の予定がないチケットを確認できます。タイムラインへドラッグした場合は、時刻、重なる予定の件数、所要時間を仮表示し、「この位置に保存」するまで確定しません。キーボードではチケットを選び、日付・開始・所要時間を入力して同じ操作を行えます。`Esc` または「取消」で仮配置を破棄できます。
- 予定編集の「チケットとの関連」では、既存予定をチケットへ関連付け、解除、または別チケットへ明示的に付け替えられます。付け替えは現在と変更先のチケット名を確認してから実行します。
- カードはそのままドラッグして、別の列または同じ列内の位置へ移動できます。`Esc` はドラッグを取り消します。キーボードではカードの詳細ボタンへフォーカスし、`← / →` で列移動、`↑ / ↓` で列内の順序変更ができます。
- ボード上のカードはタイトル、優先度バッジ、タグバッジを表示します。タグは優先度と異なる配色で、複数ある場合は折り返して表示します。期限、見積時間、チェックリスト、予定・Focus実績はカードの詳細で確認します。
- Doneへ移すと完了日時を記録し、Doneから戻すと未完了へ戻ります。この操作は関連予定やFocusを変更しません。チケットと予定のタイトル、完了、削除も暗黙には連動しません。
- アーカイブは通常ボードから隠す操作で、アーカイブ表示から復元できます。削除は別操作です。削除確認には現在の予定・Googleへの影響を表示し、直後なら同じ内容のチケットとして作り直せます。
- タイトル・説明、列、優先度、期限、タグ、通常ボード／完了／アーカイブで絞り込み、ボード順、期限、優先度、更新時刻で並べ替えられます。通常ボードにはDoneも含まれます。検索や絞り込み中は、見えていないカードの永続順序を守るため自由な移動を停止します。
- 狭い画面や文字を大きくした状態では列を潰さず、横スクロールで移動します。

チケットと予定の関連、新規予定、変更履歴はローカルSQLiteへ同じ処理で先に保存されます。既定のGoogle書込先がある場合、新規予定だけをOutboxから同期し、チケット本文や関連情報をGoogleへ送りません。オフラインでもローカル保存は確定し、再送で予定を重複作成しません。夏時間で存在しない時刻は保存せず、2回存在する時刻は早い方・遅い方を選択します。端末を移行する場合、チケットと関連を含まない現行JSONではなく、検証済みSQLiteバックアップを使用してください。

## 5. アラームと通知

自由アラームは予定から独立し、ラベル、時刻、IANA タイムゾーン、曜日、有効状態を指定します。追加・編集・複製・削除・並べ替えができます。

設定では次を個別に制御します。

- 予定と Quick Block の通知
- OS 通知
- アプリ内音
- Focus 通知
- タイマー終了通知
- スリープ復帰後に補償する猶予（既定10分）
- 1回に再生する最大件数（既定3件）

テーマ、操作、終了動作、通知、Focus は「既定値へ戻す」で初期値をフォームへ読み込めます。Google 接続と予定データは変更されず、内容を確認して「設定を保存」するまで設定値は確定しません。

テーマは「システム設定に合わせる」「ライト」「マイルド」「ダーク」から選べます。「マイルド」は、ライトより明るさと彩度を抑え、ダークより明るいセージグレー基調の配色です。テーマは「設定を保存」した直後に全画面へ反映され、次回起動時も復元されます。

OS 通知権限が未許可なら設定画面に状態と許可操作を表示します。拒否中は「通知される」とは表示しません。通知判定は永続台帳に記録し、同じルールと発火時刻を再起動後も重複配信しません。
自由アラームの指定時刻が DST により存在しない、または2通りに重複する日は、時刻を黙ってずらさず配信を見送ります。見送った理由は「データと診断 > 通知配信履歴」で確認できます。

重要: アプリが完全終了している間は通知できません。ウィンドウを閉じても通知を継続する場合は「設定 > アプリを閉じたとき > トレイへ格納」を選びます。トレイの「終了」または「完全終了する」を選ぶと通知 worker も停止します。

## 6. タイマーとストップウォッチ

「タイマー」では、複数のカウントダウンを追加し、それぞれに任意のラベルと1秒〜7日の時間を設定できます。各タイマーは独立して開始、一時停止、再開、リセット、削除できます。計測中または一時停止中のラベル・設定時間を変える場合は、先にリセットしてください。

現在並んでいるタイマーのラベルと設定時間は、名前付きの「タイマー構成セット」として保存できます。セットを適用すると既存タイマーを残したまま新しい停止中タイマーとして追加し、セットを削除しても現在のタイマーには影響しません。実行途中の残り時間や状態はセットへ保存しません。

「ストップウォッチ」は独立した画面にあり、開始、一時停止、再開、0へのリセットに対応します。タイマーとストップウォッチの状態はこの端末へ保存され、アプリ再起動後に回復します。タイマー終了には既存のOS通知・アプリ内音設定を使い、同じ実行を重複通知しません。アプリが完全終了している間は通知できません。

## 7. Focus

「フォーカス」で作業を開始し、一時停止、再開、休憩への移行、終了を操作します。開始前に今日の予定へ紐付けられます。作業・休憩・長休憩の長さ、自動開始、通知は設定で変更できます。

状態と履歴は端末へ保存され、画面を開き直しても回復します。当日の作業時間と遷移履歴は Focus 画面で確認できます。予定へ紐付けた完了作業時間は、予定の編集画面に「Focus実績」として全期間合計で表示します。

予定がチケットへ関連している場合、Focus開始時点の関連先へ作業実績を固定して帰属します。開始後に関連解除、別チケットへの付け替え、アーカイブ、削除を行っても、過去実績は移動しません。一時停止と休憩は実績へ含めず、内部では秒単位、画面では分単位に丸めて表示します。チケット詳細では見積、今後の予定、全予定、Focus実績、残り、実績差と直近履歴を確認できます。

チケット詳細から開始する場合は、関連予定ごとの「この予定でFocus開始」を使います。Doneのチケットでは「完了のまま開始」または「再開して開始」を明示的に選びます。Focus開始・終了だけでチケットをIn ProgressやDoneへ自動移動しません。予定を選ばない、またはTicket未関連の予定を選ぶと実績は未帰属になります。過去の未帰属実績を後から手動で割り当てる操作は現在未対応です。

## 8. Google Calendar を接続する

Google接続設定を含む個人用ビルドでは、通常の利用者によるOAuth JSONのダウンロードや読み込みは不要です。ビルドを用意する人は、追跡外のローカル設定からclient IDをビルドへ渡し、client secretをOS秘密ストアへ一度だけ登録します。初回準備は [`docs/guides/google-calendar-oauth.md`](docs/guides/google-calendar-oauth.md) を参照してください。

1. 「設定 > Google カレンダー」で「Google カレンダーに接続」を選びます。
2. 既定ブラウザで接続するGoogleアカウントと要求権限を確認し、許可します。
3. Google の同意画面を完了すると、`127.0.0.1` の一時 callback でアプリへ戻ります。
4. 読み込むカレンダーと、書き込み可能な既定カレンダーを選びます。

このビルドに接続設定が含まれない場合は、ローカル予定を維持したまま接続できない理由と回復手順を表示します。開発者向けの詳細では、従来どおり自分のDesktop OAuth JSONを読み込んで上書きできます。認証失効後の再接続では、既存のローカル予定、カレンダー選択、同期紐付けを削除しません。

ローカル変更は SQLite へ先に確定し、Outbox から Google へ反映します。オフラインでもローカル保存は完了します。同期状態は上部と「データと診断」で確認できます。

各カレンダーには「未同期」「同期中」「同期済み」「再試行予定」「確認が必要」「再接続が必要」を表示します。一つのカレンダーで権限や予定形式の問題が起きても、既存のローカル予定と前回の同期位置を保持し、読み取り可能な他のカレンダーは同期を続けます。「空き時間のみ」の権限では予定本文を取得できないため、同期対象には選べません。

Googleの複数ルール、追加日、除外日を含む繰り返し予定も表示と同期を継続します。条件を失わないよう、この形式の予定はアプリ内では読み取り専用です。予定を選ぶと理由を表示するので、系列の変更・削除はGoogleカレンダー側で行ってから再同期してください。

手動同期の実行中は「同期を取り消す」を選べます。取り消しは次の安全な処理境界で停止するため、すでに Google またはローカルへ確定した項目は戻しません。未反映項目は Outbox に保持され、同じ操作 ID の再試行でも予定を重複作成しません。

同じフィールドを両側で変更した場合や削除競合は自動上書きせず、競合画面でフィールドごとにローカル／Google を選びます。認証失効時は再接続、レート制限や一時障害は次回時刻付きで再試行します。

### Google Tasks とTicketを同期する

Google Tasks同期は、Google側でも確認したい行動項目をTicketと対応させる機能です。Google Tasksを使わない場合は無効のままにでき、Calendar同期とLocal Ticketは変わりません。

1. 既存のCalendar接続にTasks権限がない場合は「Calendar + Tasksを再同意」を選びます。新しい権限とCalendar／Task List取得がすべて成功するまで、以前のCalendar認証情報を置き換えません。
2. 「Tasks同期を有効化」し、同期するTask Listと「新規Ticketの同期先」を選びます。
3. Ticket詳細で同期先Task Listを選びます。設定後は反映待ち、同期済み、オフライン、再試行待ち、競合などを同じ欄で確認できます。

同期するのはtitle、notes、dueの日付、完了、親子、Task Listです。priority、見積、tags、Scheduleとの関連、Focus実績はLocal専用で、Google notesへ埋め込みません。Googleのdueは時刻を保持しないため、作業時刻はScheduleで管理します。割り当てられたGoogleタスクは初期版の対象外です。

通常は起動時、アプリ復帰時、5分間隔で差分同期します。「今すぐ同期」はCalendarとTasksの通常同期、「完全照合」は選択Listの全件を再確認します。全ページ取得とローカルtransactionが完了するまでwatermarkを更新しません。

同じ項目を両側で変えた場合は、基準／Local／Googleを確認し、Localを再送、Googleを採用、または同期解除を選びます。作成結果が通信切断で不明な場合は重複防止のため自動再作成せず、Google側を確認してから同期解除します。同期解除は両側を保持します。Google側Taskの削除は別の確認操作で、Local Ticketは保持します。

Google 接続を解除すると、その接続に残る未送信 Outbox は無効化され、未送信の予定はこの端末だけの予定へ戻ります。後から別の Google アカウントやカレンダーへ接続しても、以前の未送信操作を自動転送しません。

## 9. アナログ時計、Compact Window とトレイ

ヘッダーの「今日」の右にある小さなアナログ時計は、予定の日付を移動していても端末の現在時刻を示します。短針・長針・秒針を一目で確認したいときに使い、クリックまたはキーボードで選ぶと「アナログ時計」ウィンドウが開きます。すでに開いている場合は同じウィンドウを手前へ戻すため、重複しません。

専用ウィンドウは時計盤が短辺の約90%を占める、時計を主役にした表示です。右上のピンボタンを選ぶと、設定画面を開かずにアナログ時計だけの「常に手前」を切り替えられます。押された表示のときは固定中で、ボタンにポインターを合わせると現在可能な操作を確認できます。その右の「時計の設定を開く」ボタンを選ぶと、時計盤の上に設定が表示されます。設定を閉じるには「時計の設定を閉じる」、時計盤の外側、または Escape キーを使います。

設定画面では次を操作できます。

- 「自動」は 06:00 から 17:59 をライト、18:00 から 05:59 をダークにします。ライト／ダークを選ぶと時間帯に関係なく固定します。
- 「サイズ変更」は 1.0、1.5、2.0、2.5 倍を順に切り替えます。画面より大きい場合は表示領域内へ収まる大きさに制限されます。ウィンドウ枠をドラッグすると、280px四方まで時計を表示する内容領域の縦横比を 1:1 に保ったまま拡大・縮小し、時計盤もウィンドウに合わせて動きます。狭い表示では左上のデジタル表示が時刻中心へ切り替わります。時計専用ウィンドウのため最大化はできません。
- 「秒針音」は初期状態では無効です。有効にすると、参照アプリと同じ短い機械式クリックの音響特性を再構成した音を 1 秒ごとに流します。音量は 0〜100%で指定できますが、突然大きな音にならないよう実出力は控えめな上限を設けています。
- 音は OS の既定出力デバイスを使います。このアプリから出力先は変更しません。聞こえない場合は OS の音量、消音、既定出力を確認し、「秒針音」をいったん無効にしてから再度有効にしてください。
- 「常に手前」は右上のピンボタンと同じ設定です。アナログ時計だけを他のウィンドウより手前に保ち、次回も設定を復元します。作業を隠す場合はどちらかの操作で解除してください。

時計は音を無効にしたままでもすべての時刻情報を表示します。システムの時刻や timezone を変更した場合、またはスリープから復帰した場合は、経過秒を足し続けず端末の現在時刻を取り直します。ウィンドウを閉じても予定、通知、Google 同期は変更されません。開けない場合はメインウィンドウを表示したまま、ヘッダーの時計をもう一度選んでください。

「コンパクト表示」は現在、次、残り、Focus を小さな別ウィンドウで表示し、常に手前を設定できます。トレイから Today、Quick Add、Focus、同期、終了を操作できます。二重起動時は既存ウィンドウを表示し、同期／通知 worker の重複を防ぎます。

## 10. バックアップ、移行、診断

「設定 > データ」から JSON export / import、バックアップ作成、復元準備、旧 DB import、全データ削除を操作します。

- import と旧 DB import は件数・警告・fingerprint を読み取り専用 preview で確認してから単一 transaction で確定します。JSONにはタイマーのラベル・設定時間と構成セットを含めますが、タイマーとストップウォッチの実行途中の状態は含めません。
- チケットとチケット―予定の関連は現行JSON export / importの対象外です。チケット本体、列順、親子、タグ、チェック項目、履歴、関連予定を一貫して移行・復元する場合は、検証済みSQLiteバックアップを使用してください。JSON import previewにもこの制約を表示します。
- JSON export と手動バックアップは実行中に取り消せます。export の未完成 `.part` と未検証バックアップは削除され、完成済みファイルと現在 DB は変更しません。バックアップの SQLite 処理が進行中の場合は、処理終了後に未完成世代を削除します。
- 復元準備では検証済みバックアップを次回起動用に staging します。再起動時は staging 上の候補へ integrity check、migration、smoke query を実行し、すべて通過してから現在 DB を一時退避して切り替えます。候補の migration に失敗した場合は現在 DB を切り替えません。切替途中で中断した場合は次回起動時に退避済み DB を回復します。
- 全データ削除は画面に示された確認文の完全一致が必要です。Google credential も OS 秘密ストアから削除します。

「データと診断」にはアプリ／DB version、予定件数、削除待ち、Outbox、競合、整合性、最終バックアップ、通知台帳に加え、Google Tasksの選択List数、同期Ticket数、反映待ち、競合、最終成功、エラー分類、次回再試行を表示します。「バージョン情報をコピー」と「マスク済み診断 JSON」には予定／Ticket本文、メール、calendar / event / task / list ID、token、絶対パスを含めません。

## 11. 既知の配布制約

- 初期配布物は個人利用向け unsigned build です。OS の警告が表示される場合があります。
- 自動 updater は無効です。更新前にバックアップし、新しい installer を手動実行します。
- Linux、mobile、Web、Outlook、iCloud、CalDAV、チーム共有、AI 自動スケジューリングは対象外です。
- macOS / Windows の通知、credential store、OAuth loopback、sleep / resume は対象 OS の実機 release smoke を通してから第三者へ配布してください。
